"""Composes package primitives — an intra-package edge (pipeline -> math_ops) for the arch gate to chew."""

from {{ package_name }}.math_ops import MathOps


class Pipeline:
    """Composes `math_ops` primitives — demonstrates an internal downward edge within the package."""

    @staticmethod
    def clamped_mean(values: list[float], *, low: float, high: float) -> float:
        """Mean of `values`, clamped into [`low`, `high`] via two primitives."""
        return MathOps.clamp(MathOps.mean(values), low=low, high=high)

# {{ project_name }} — PLAN (source of truth)

This is the canonical plan. Read it at session start before proposing work. Substance lives here;
ordered execution status lives in `ROADMAP.md`.

## Goal

_(placeholder)_ The north star for {{ project_name }} — what problem it solves and for whom.

{% if enable_ml %}## Headline metrics

_(placeholder)_ Decide these up front; the comparison IS the result, not a leaderboard number.

| metric | baseline | target | current |
|---|---|---|---|
| _(tbd)_ | — | — | — |
{% else %}## Success criteria

_(placeholder)_ What "done" looks like — the observable outcomes that decide the project worked.
Name them up front so scope creep and gold-plating are visible against a fixed bar.
{% endif %}

## Structure

- {% for p in packages.split(",") %}`{{ p | trim }}/`{% if not loop.last %}, {% endif %}{% endfor %} — your package{% if packages.split(",")|length > 1 %}s{% endif %} (the guardrails' target; organise modules as needed).
- `tests/` — unit / integration / e2e.

## Approach

{% if enable_ml %}_(placeholder)_ The argument from public evidence → our-data statistics → method. Derive
parameters; no magic numbers.{% else %}_(placeholder)_ How the goal is met — the key design decisions and the rationale behind each.{% endif %}

## Open questions

_(placeholder)_

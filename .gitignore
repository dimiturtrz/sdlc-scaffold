# scaffold's own tooling artifacts (its E2E suite runs uv here)
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
uv.lock

# local scratch instance generated from the template for manual poking — regenerable, never committed
/sdlc_test/

# self-scaffolding: the scaffold maps its own engines. graph.json IS committed (the diffable architecture);
# the ~700KB interactive viewer shell is regenerated locally / in the pages workflow — not committed.
sdlc-devtools/docs/architecture/index.html
.grimp_cache/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
